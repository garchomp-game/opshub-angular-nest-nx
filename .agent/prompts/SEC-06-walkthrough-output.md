# SEC-06 ウォークスルー出力

// turbo-all

作業のウォークスルーを以下のパスに出力してください:
`opshub/.agent/prompts/walkthroughs/SEC-05-walkthrough.md`

## 含める内容
- `.github/workflows/ci.yml` への変更内容
- `--max-warnings` で設定した値とその根拠
- ローカルでの `pnpm nx lint web --max-warnings=N` の実行結果
